{
  "name": "metaclicker",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^15.2.1",
    "react-dom": "^15.2.1"
  },
  "devDependencies": {
    "source-map-loader": "^0.1.5",
    "ts-loader": "^0.8.2",
    "typescript": "^1.8.10",
    "typings": "^1.3.2"
  },
  "scripts": {
    "postinstall": "typings install",
    "deploy": "webpack -p && git --git-dir=../metaclicker.github.io/.git --work-tree=dist/ add -A && git --git-dir=../metaclicker.github.io/.git commit -m deploy && git --git-dir=../metaclicker.github.io/.git push"
  }
}
